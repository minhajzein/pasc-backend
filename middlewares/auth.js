const jwt = require('jsonwebtoken')

module.exports = {
    verifyUser: async (req, res, next) => {
        try {

        } catch (error) {
            console.log(error);
        }
    },
    verifyAdmin: async (req, res, next) => {
        try {

        } catch (error) {
            console.log(error);
        }
    }
}